<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Use the dedicated adaptive foreground (Android robot without background).
        This ensures the monochrome layer works correctly for fluid-cloud surfaces
        while the full icon is preserved for legacy launchers.
    -->
    <item android:drawable="@drawable/ic_launcher_foreground_adaptive" />
</layer-list>
